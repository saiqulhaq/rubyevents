<div role="tablist" class="mb-12 tabs tabs-bordered">
  <% if event.meetup? %>
    <%= active_link_to "Events", event_events_path(event), class: "tab", active_class: "tab-active", role: "tab" %>
  <% end %>

  <% if event.talks.any? %>
    <%= active_link_to "Talks", event_talks_path(event), class: "tab", active_class: "tab-active", role: "tab" %>
  <% end %>

  <% if event.meetup? %>
    <%= active_link_to "Videos", event_videos_path(event), class: "tab", active_class: "tab-active", role: "tab" %>
  <% end %>

  <% if event.speakers.any? %>
    <%= active_link_to "Speakers", event_speakers_path(event), class: "tab", active_class: "tab-active", role: "tab" %>
  <% end %>

  <% if event.schedule.exist? %>
    <%= active_link_to "Schedule", event_schedules_path(event), class: "tab", active_class: "tab-active", role: "tab" %>
  <% end %>

  <% if event.sponsors.any? %>
    <%= active_link_to "Sponsors", event_sponsors_path(event), class: "tab", active_class: "tab-active", role: "tab" %>
  <% end %>

  <% if false %>
    <a role="tab" class="tab">Organizers</a>
    <a role="tab" class="tab">Photos</a>
  <% end %>
</div>
